services:
  - type: web
    name: chanjoongx-prophet-hacks
    runtime: python
    plan: free
    region: oregon
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false       # primary key — set in Render dashboard
      - key: ANTHROPIC_API_KEY
        sync: false       # tier-2 fallback (direct Anthropic, no web search)
      - key: OPENAI_FALLBACK_API_KEY
        sync: false       # tier-3 fallback (direct OpenAI, no web search)
      - key: FORECAST_MODEL
        value: anthropic/claude-sonnet-4:online
      - key: PYTHON_VERSION
        value: "3.12.5"
